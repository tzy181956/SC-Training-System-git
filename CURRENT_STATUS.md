# CURRENT_STATUS

## 项目当前阶段

当前处于 `V1 可运行版 + 真实数据验证阶段`。  
项目已经从纯示例数据演示，进入“用真实女篮青年队测试数据验证业务链路”的阶段。

## 当前版本 / 当前状态

- 当前可视为 `V1-dev / real-data-validation`
- 本地数据库 `backend/training.db` 已导入 `2026-03-14` 女篮青年队真实测试数据
- 示例运动员、测试记录、计划分配、训练 session、训练组记录已清空
- 训练模板和动作库保留
- 初始化链路已改为非破坏式，不再自动清库或灌入 demo 数据

## 已完成模块

- 动作库管理
- 训练模板管理
- 计划分配概览与批量分配
- 计划取消分配
- 训练模式执行
- 训练 session 自动创建 / 复用
- 当前组提交、历史组即时修改
- 自动完成动作 / 自动完成 session
- 测试数据记录与查看
- Windows 一键初始化 / 启动脚本
- iPad 局域网访问基础链路
- 真实 Excel 测试数据一次性导入脚本

## 当前可运行能力

- 管理模式下可以管理运动员、动作、模板、计划分配、测试数据
- 训练模式下可以按队员 / 计划进入训练并录入数据
- 支持共享 iPad 的训练录入场景
- 训练录入支持当前组确认提交、历史组修改、自动状态同步
- 测试数据已支持：
  - 运动员主档身体指标
  - 测试记录数值结果
  - 时间文本结果 `result_text`
  - 力量体重比运行时计算

## 当前主要问题 / 风险

- 旧文档和部分页面仍存在中文乱码，仓库文本一致性一般
- 数据库没有正式迁移机制，目前依赖 `startup schema sync + init_db.py`
- 真实数据已写入当前本地库，但跨电脑时如果重建数据库，需要重新执行导入脚本
- 当前测试数据页仍偏“管理录入页”，不是最终分析页
- 计划模板仍沿用原有项目/队伍引用，真实数据和旧模板字典共存

## 最近一次重要进展

- 扩展了运动员主档字段：`height / weight / body_fat_percentage / wingspan / standing_reach`
- 扩展了测试记录字段：`result_text`
- 新增真实数据导入脚本：
  - [import_real_test_data.py](C:\Users\Tian Ziyu\Documents\New project\backend\scripts\import_real_test_data.py)
- 已把 `测试结果带排名.xlsx` 中的 `2026-03-14` 女篮青年队真实数据导入系统
- 当前库内状态：
  - `16` 名真实队员
  - `368` 条测试记录

## 当前最高优先级事项

1. 在前端实际核对导入后的运动员和测试数据展示是否完全符合 Excel
2. 清理剩余 demo 假设和乱码文本，确保进入真实使用状态
3. 把“真实数据导入”沉淀成稳定流程，跨电脑时可以重复执行

## 暂时不要动的部分

- 训练模板和动作库：当前要求是保留，不要为了清理真实数据顺手重置
- 训练模式主流程：当前已较复杂，除明确 bug 外不要顺手重构
- 启动脚本跨电脑自修复逻辑：已做过适配，改动前要先验证现有行为
