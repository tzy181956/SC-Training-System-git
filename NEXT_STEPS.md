# NEXT_STEPS

## 2026-05-16 下一阶段优先级

Alembic drift 收口、FK migration `c9d0e1f2a3b4`、FK orphan 预检脚本和训练端离线草稿 Playwright E2E 已完成并合并。当前下一阶段优先级调整为：

1. 统一服务器手动部署前检查和部署流程；
2. 在服务器生产库或生产库快照上运行 `python scripts/check_fk_orphans.py`；
3. orphan=0 后执行服务器 `python scripts/migrate_db.py ensure`，并检查 `/health`、`/ready`、`/ready/deep`；
4. 使用真实 iPad / 浏览器抽测训练端、监控端、训练报告；
5. 后续再进入产品功能开发，例如训练报告分析、监控端优化、计划分配 UI、性能优化。

已完成项不再作为待办推进：

- Alembic metadata-only drift 收口；
- Alembic business metadata 决策；
- FK orphan 预检脚本；
- FK migration `c9d0e1f2a3b4`；
- 训练端离线草稿 Playwright E2E；
- 新电脑本地最终验收。

## 当前优先级

### 1. 文档体系与代码事实继续对齐

目标：

- 把本轮文档重修收口完
- 保证后续每次功能变更都会同步更新权威文档

完成标准：

- 根目录权威文档与代码事实一致
- `docs/` 下专题文档已标明“当前有效 / 历史阶段”
- 启动器与排障文案统一为“本地开发/初始化工具”口径，并与服务器部署说明保持一致

### 2. 继续收口测试评分与雷达图

目标：

- 把评分模板、自动计算、雷达图对比、筛选交互收口到稳定状态

完成标准：

- 评分模板 CRUD 稳定
- 自动计算链路稳定
- 个人与团队对比图口径一致
- 日期、队伍、模板联动逻辑清晰

### 3. 继续优化大数据页面性能

目标：

- 继续把动作库、测试数据总库、训练模板等大页面收口到分页/按需加载模式

完成标准：

- 管理首页与大列表页面不再依赖全量首屏加载
- 列表与详情请求分离
- 删除、保存后只刷新当前筛选范围

### 4. 监控端与训练执行链路持续回归

目标：

- 保持训练模式、监控端、同步状态和训练报告之间的状态一致性

完成标准：

- 训练状态在训练端、监控端、报表端口径一致
- 同步异常可见且可追踪
- 回归中不破坏本地草稿和补传链路

### 5. 服务器更新与迁移流程继续标准化

目标：

- 把生产更新、迁移、备份和恢复流程固定成开发者可重复执行的标准动作

完成标准：

- 每次更新都按部署文档执行
- migration、备份、服务重启、健康检查都有固定步骤
- 服务器与本地环境职责边界清晰
