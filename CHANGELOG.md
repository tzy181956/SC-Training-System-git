# CHANGELOG

## Unreleased / V1-dev

### Added

- 增加训练模式与训练 session 主流程。
- 增加计划分配概览、分组展示与批量取消分配。
- 增加训练模式下当前组提交、历史组即时修改。
- 增加训练状态颜色提示、自动完成动作、自动完成 session。
- 增加更稳的 Windows 初始化与启动脚本链路。
- 增加运动员主档身体字段：
  - `height`
  - `weight`
  - `body_fat_percentage`
  - `wingspan`
  - `standing_reach`
- 增加测试记录展示字段 `result_text`。
- 增加真实 Excel 数据导入脚本 `backend/scripts/import_real_test_data.py`。
- 增加运行时 schema 同步逻辑 `backend/app/core/schema_sync.py`。
- 增加训练模式顶部访问入口卡片，支持显示局域网访问地址、复制按钮和二维码。
- 增加仓库级编码防线：
  - `.editorconfig`
  - `.gitattributes`
  - `scripts/check_text_encoding.py`
  - `docs/text-encoding.md`

### Changed

- 重写根目录 `AGENTS.md`，将仓库协作约束升级为“第一阶段核心稳定性重构”导向：明确训练端本地草稿、同步状态、session 状态流、正式迁移、备份恢复、危险操作与日志规则，并规定每次任务输出必须给出风险、验收和自测信息。
- 训练模板彻底删除 `rest_seconds / 休息时间`：前端编辑卡片、保存 payload、后端模型与 schema、运行时数据库列同步全部移除，历史数据随删列一并清理。
- 训练模板和训练展示不再暴露“顺序”输入或编号，统一改为按卡片排列顺序执行与展示。
- 训练模板动作卡片中的“动作备注”输入框收紧为单行高度，减少编辑区占用空间。
- 训练模板动作卡片改为“一级分类 + 二级分类 + 可搜索动作”三段式选动作，并将第二行参数区重排为紧凑布局，训练目标改为自由输入。
- 调整训练模板动作卡片的桌面端栅格断点和列宽比例，避免一级分类、二级分类、动作搜索及参数区过早退化成单列。
- 将训练模板动作卡片的桌面端两行参数区改为稳定的横向工具条布局，确保一级分类、二级分类、动作搜索以及组数/次数/负荷等字段保持同排展示。
- 训练模板动作搜索从原生 `datalist` 改为自定义可搜索下拉，关闭状态统一为类下拉框样式，打开后在同宽面板内搜索和选择动作。
- 计划分配上半区改为“左主右辅”的紧凑概览：左侧进行中的计划占主导，右侧未分配队员收口为更窄的滚动侧栏，空态和统计文案同步压缩。
- 计划分配预览改为“队员名单 + 单份模板预览 + 异常提示区”：同一模板不再按人重复展开，固定重量与百分比动作主预览只显示规则，缺少测试基准的队员和动作改为集中提示。
- 计划分配上半区概览从“仅查看日期当天”改为“查看日期当天 + 未来有效计划”，并为计划组增加“进行中 / 即将开始”状态标识；未分配人数与名单同步改为按当前日期往后无有效计划计算。
- 计划分配页将取消已有计划的主入口收口到上方“当前与后续计划”分组：支持按组内选择队员后删除分配，并要求二次确认；下方第一步队员卡片不再承担删除旧计划职责，只保留新增分配选择。
- 计划分配左侧第二步不再重复展开模板动作预览，右侧改为唯一正式预览区；模板一经选中即可先在右侧查看模板内容，待队员和时间范围齐备后再升级显示完整分配预览。
- 计划分配页删除“刷新预览”按钮，右侧预览完全改为自动更新；分配备注也纳入自动预览刷新。
- 训练模式改为“点计划即训练”，不再依赖开始训练前的确认页。
- 当前组恢复为手动确认提交，历史组保持即时修改。
- 训练录入区改成更适合 iPad 的大按钮交互。
- 训练模式左侧队员计划改为常驻展开。
- 训练模式侧边栏新增按日期联动的队伍筛选。
- 测试数据页改为支持展示时间文本和力量体重比计算。
- 运动员页扩展为可编辑身体主档数据。
- 缺少测试基准时，计划分配不再被阻断，改为“训练时录入”。
- 启动脚本的自动初始化链路不再覆盖真实业务数据。
- 真实数据导入脚本改为先要求输入固定确认词，再执行批量清理和导入。
- 共享数据库文件 `backend/training.db` 纳入 Git 跟踪，跨电脑同步改为依赖提交数据库文件。
- 动作库重构为以 Excel `exos_action_library_tagged_for_codex.xlsx` / `动作库_标签版` 为唯一权威源的结构化动作库。
- 动作库页面收口为日常维护模式，网页端只保留浏览、筛选、新建、编辑、删除；导入能力仅保留为维护级脚本和 API。
- 动作库左侧筛选面板改为使用后端 facets 聚合接口，不再由前端从全量动作本地现算。
- 动作库和运动员页的顶部筛选区统一为“搜索框单独一行，分类/筛选控件下一行”的布局。
- 训练模式头部改为紧凑单行布局，访问地址卡片、当前模式和模式切换统一收口到同一行。
- 训练模板新增动作时，`主项动作` 和 `启用自动调重` 默认改为不勾选。
- 训练模板页改为“顶部统一保存”模式，不再要求对单个动作卡片单独点击保存。
- 训练模板页去掉“快速添加动作”表单，改为仅保留“添加动作”按钮，新增后直接在动作卡片内完成选择和填写。
- 动作库页面去掉用户可见的 `EXOS` 字样，只保留内部数据来源说明。
- 删除动作库中的 `负荷类型 / load_profile` 字段，保留真正仍在使用的步长等字段。
- 当前数据库中的运动员性别统一更新为 `女`。
- 重写并恢复根目录协作文档与 `backend/README.md` 的 UTF-8 中文内容，使其与当前实现重新对齐。
- 恢复 `backend/scripts/import_real_test_data.py` 中被破坏的中文路径、表头映射和说明文本，并同步去掉已废弃的 `training_level` 写入。

### Fixed

- 修复跨电脑复制 `.venv` 后因旧解释器绝对路径导致初始化失败的问题。
- 修复 Node `spawn EPERM` 误判，明确区分环境限制与项目问题。
- 修复模板页面“新增动作卡片看起来可点但实际无反应”的交互问题。
- 修复计划分配预览中固定重量动作被误判为“缺少测试基准”的问题；前端改为只对后端返回的 `missing_basis` 状态触发提示和禁用提交。
- 修复计划分配概览接口被动态路由误拦截的问题。
- 修复训练最后一组提交后动作状态不同步的问题。
- 修复历史 session 中已满组但仍显示 `in_progress` 的问题。
- 修复训练录入中默认伪造 `20` 公斤的问题。
- 修复真实 Excel 导入时把“全队平均”汇总行误导入为运动员的问题。
- 修复管理模式与训练模式中多处侧栏/列表区域无限向下增长的问题，改为容器内滚动。
- 修复外层壳层高度未被真正限制导致内部滚动未生效的问题。
- 修复动作库与运动员列表中部分中文名称/描述在滚动容器内出框的问题。
- 调整动作库与运动员左侧列表卡片的自适应宽度、文本层级与高度，减少中文被硬切和内容被裁掉的问题。
- 统一前端多处列表/卡片的自适应文本规则，覆盖动作库、运动员、测试数据、训练模式侧栏、模板动作卡片和训练报告卡片。
- 修正测试数据页折线图的取数逻辑，只展示当前运动员在当前测试项目下的历史趋势。
- 为测试数据页图表增加当前测试项目的全队平均趋势线，并升级为“最高/最低区间阴影 + 全队平均线”的表现方式。
- 重构测试数据页区间带实现，增加最高/最低边界线、更稳定的区间阴影和 tooltip。
- 为测试数据页补充单日期点场景下的窄竖向区间带，避免仅有一个测试日期时区间完全不显示。
- 修复 `/api/exercises` 因历史动作记录 `structured_tags` 和 `search_keywords` 为空导致接口返回 `500`、动作库页面整体空白的问题。
- 修复动作库默认无筛选时一次性渲染全部动作导致初始加载卡顿的问题，改为先筛选再展示结果。
- 修复动作库中间列表、右侧详情区缺少独立滚动的问题。
- 修复动作库筛选结果只有一条时卡片被拉伸占满整个区域的问题。
- 修复训练模式顶部访问入口与模式区因布局挤压而掉到左下角的问题。
- 修复训练模式和访问卡片中的中文乱码文案。
- 修复训练模式头部中日期/队伍筛选与二维码卡片反复串行换行的问题，改为稳定的同一行紧凑布局。
- 修复训练模板中未启用自动调重时仍显示进阶逻辑、训练执行中仍继续给出自动调重参考建议的问题。
- 修复训练模板页单动作卡片必须单独保存、删除模板缺少统一按钮样式与确认、保存模板后缺少自动消失反馈提示的问题。
- 修复训练模板页“先在顶部快速添加区选动作再新增”的冗余流程，改为直接新增空白动作卡片编辑。
- 修复批量更新运动员性别时写入错误字符导致页面显示异常的问题。
- 修复仓库内多份协作文档和真实数据导入脚本出现的内容级中文乱码问题，并明确区分“终端显示异常”和“文件内容损坏”。

### Notes

- 当前项目仍未引入正式数据库迁移框架，模型扩展主要依赖：
  - `backend/scripts/init_db.py`
  - `backend/app/core/schema_sync.py`
  - `backend/scripts/import_real_test_data.py`
- 动作库初始化导入已完成，日常维护以网页端增删改查为主。
- 训练模式访问入口依赖 `scripts/start_system.bat` 生成的运行时地址配置，二维码在前端本地离线生成。
- 后续检查中文文件时，不再以 PowerShell 默认输出作为最终依据，统一以编辑器或 Python UTF-8 读取为准。
